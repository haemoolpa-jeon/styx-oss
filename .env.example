# Styx 서버 설정

# 서버 포트 (기본: 3000)
PORT=3000

# CORS 허용 도메인 (쉼표로 구분)
# 설정하지 않으면 같은 origin만 허용
# 예시:
# CORS_ORIGINS=http://localhost:3000,http://192.168.1.100:3000,https://your-domain.com

# 데스크톱 앱에서 접속하려면 해당 IP/도메인 추가 필요
# CORS_ORIGINS=http://localhost:3000,tauri://localhost

# HTTPS 리다이렉트 (프로덕션에서 true로 설정)
# Nginx/로드밸런서 뒤에서 실행 시 x-forwarded-proto 헤더 필요
FORCE_HTTPS=false

# Node 환경 (development | production)
NODE_ENV=development
