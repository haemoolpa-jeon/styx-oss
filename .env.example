# Styx Server Configuration

# Server port (default: 3000)
PORT=3000

# CORS allowed origins (comma-separated)
# If not set, only same-origin requests are allowed
# Example:
# CORS_ORIGINS=http://localhost:3000,https://your-domain.com,tauri://localhost

# HTTPS redirect (set to true in production behind reverse proxy)
# Requires x-forwarded-proto header from Nginx/load balancer
FORCE_HTTPS=false

# Node environment (development | production)
NODE_ENV=development

# TURN server settings (required for NAT traversal)
# Without TURN, users behind strict NAT/firewall may not connect
# See docs/AWS_LIGHTSAIL_DEPLOY.md for Coturn setup
# TURN_SERVER=your-turn-server-ip
# TURN_SECRET=your-coturn-static-auth-secret
